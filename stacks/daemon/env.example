# OpenClaw Gateway Environment Variables
# Copy to ~/.openclaw/env and fill in values

# Gateway authentication token (required for production)
OPENCLAW_GATEWAY_TOKEN=your-secure-token-here

# Model provider API keys (at least one required)
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# Optional: override default gateway port
# OPENCLAW_GATEWAY_PORT=18789

# Optional: override default workspace path
# OPENCLAW_WORKSPACE=/home/openclaw/.openclaw/workspace

# Optional: Tailscale for remote access
# TS_AUTHKEY=tskey-auth-...
